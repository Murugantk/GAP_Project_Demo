name: variable

 
on: 
  workflow_dispatch
env:
    MY_VAR: workflow
jobs:
  variables:
    env:
      MY_VAR: steps
    runs-on: ubuntu-latest
    steps:
    - name: print
      run: |
           echo ' ${{secrets.PASSWORD}}
      env:
        MY_VAR: steps
        
        
    
    
  
   
