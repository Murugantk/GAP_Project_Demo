name: variable

 
on: 
  workflow_dispatch
env:
    MY_VAR: workflow
jobs:
  variables:
    env:
      MY_VAR: steps
    runs-on: ubuntu-latest
    steps:
    - name: print
      run: |
           echo ' ${{env.MY_VAR}}'
           echo ' ${{vars.MY_VAR}}'
      
      env:
        MY_VAR: steps
        
        
    
    
  
   
