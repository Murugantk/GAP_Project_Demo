name: Setup Python

on:
  workflow_dispatch:
jobs:
  python:
    runs-on: ubuntu-latest
    steps: 
      - name: Setup Python
        uses: actions/setup-python@v5.6.0
        with:
          python-version:
    
      
    
